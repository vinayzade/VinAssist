# Copy to android/keystore.properties (git-ignored). Never commit the real file.
# Paths are relative to android/app.
storeFile=upload-keystore.jks
storePassword=Cenajohn007*
keyAlias=upload
keyPassword=Cenajohn007*
