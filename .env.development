# Local development. 10.0.2.2 is the Android emulator's alias for the host
# machine; use http://localhost:8000 on the iOS simulator or a LAN IP on a
# physical device (override in an untracked `.env`).
APP_ENV=development
API_BASE_URL=http://10.0.2.2:8000
